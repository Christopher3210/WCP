# 真正的性能优化说明

## 问题诊断

你的原始实现速度慢的主要原因：

### 1. 创建了太多不必要的变量
```python
# 原版：为所有(t,u)对创建变量，即使用户u不能做任务t
x = {(t,u): model.NewBoolVar(...) for t in range(n) for u in range(m)}
# 变量数量：n × m
```

如果有100个任务和100个用户，就创建了10,000个变量！但实际上很多用户根本不能做某些任务。

### 2. 创建了太多约束
```python
# 原版Binding约束：为每个用户创建一个约束
for u in range(m):
    model.Add(x[c.t1, u] == x[c.t2, u])
# 约束数量：m个
```

如果有100个用户，每个Binding约束就创建了100个子约束！

### 3. 使用了并行搜索
```python
solver.parameters.num_search_workers = 8
```

对于小实例，启动8个线程的开销远大于收益。

## 优化方案

### 优化1：预处理Authorisation约束

**目的：** 提前计算每个任务可以分配给哪些用户

```python
# 初始化：所有用户都可以做所有任务
authorized_users = [set(range(m)) for _ in range(n)]

# 处理authorisation约束
for c in instance.constraints:
    if isinstance(c, AuthorisationConstraint):
        for t in range(n):
            if t not in c.tasks:
                authorized_users[t].discard(c.user)
```

**效果：** 如果平均每个任务只能由30%的用户完成，变量数量从n×m减少到n×(0.3m)，减少70%！

### 优化2：只为可能的分配创建变量

```python
# 优化版：只为授权的(t,u)对创建变量
x = {}
for t in range(n):
    for u in authorized_users[t]:  # 只遍历授权用户
        x[t, u] = model.NewBoolVar(f'x_{t}_{u}')
```

**效果：** 大幅减少变量数量，CP-SAT求解速度与变量数量直接相关。

### 优化3：Binding约束只对共同用户

```python
# 优化版：只对两个任务的共同授权用户创建约束
common_users = authorized_users[c.t1] & authorized_users[c.t2]
if not common_users:
    model.AddBoolOr([])  # 无解
else:
    for u in common_users:
        model.Add(x[c.t1, u] == x[c.t2, u])
```

**效果：** 如果只有10个用户可以同时做两个任务，就只创建10个约束而不是100个。

### 优化4：Separation约束只对共同用户

```python
# 只对两个任务的共同授权用户禁止
common_users = authorized_users[c.t1] & authorized_users[c.t2]
for u in common_users:
    model.Add(x[c.t1, u] + x[c.t2, u] <= 1)
```

**效果：** 同样大幅减少约束数量。

### 优化5：单线程求解

```python
solver.parameters.num_search_workers = 1
```

**原因：**
- 小实例(<100任务)：求解时间<1秒，线程启动开销占主导
- 大实例(>1000任务)：才需要并行搜索

### 优化6：使用简单约束

```python
# 对于AC1/AC2/AC5，使用简单的if-then约束
b = model.NewBoolVar(...)
model.Add(sum(...) >= 1).OnlyEnforceIf(b)
model.Add(sum(...) == 0).OnlyEnforceIf(b.Not())
```

**原因：** 虽然AddMaxEquality看起来更优雅，但在某些情况下，简单约束的传播速度可能更快。

## 性能对比

假设一个实例：
- 100个任务
- 50个用户
- 平均每个任务只能由20个用户完成（40%）
- 10个Binding约束

### 原版：
- 变量数：100 × 50 = 5,000
- Binding约束数：10 × 50 = 500
- 线程数：8（小实例浪费）

### 优化版：
- 变量数：100 × 20 = 2,000（减少60%）
- Binding约束数：10 × 5 = 50（假设平均5个共同用户，减少90%）
- 线程数：1

**预期提速：** 5-10倍

## 测试方法

在notebook中运行：
```python
for batch_index in range(1, 8):
    test_batch(batch_index)
```

预期结果：
- Batch 1: 应该在0.1秒左右
- Batch 2: 应该在0.7秒左右
- Batch 3-7: 应该都能在参考时间内完成

## 如果还是慢怎么办？

### 方案1：更激进的预处理
分析Binding/Separation约束，进一步减少可能的分配。

### 方案2：启发式搜索
添加搜索提示，引导CP-SAT更快找到解。

### 方案3：分而治之
如果实例有明显的子问题，可以分开求解。

### 方案4：调整求解器参数
```python
# 尝试不同的搜索策略
solver.parameters.search_branching = cp_model.FIXED_SEARCH
solver.parameters.linearization_level = 2
```

## 调试技巧

### 查看模型统计信息
```python
print(f"变量数: {model.NumVariables()}")
print(f"约束数: {model.NumConstraints()}")
```

### 查看求解时间分解
```python
solver.parameters.log_search_progress = True
```

这会显示：
- 预处理时间
- 搜索时间
- 是否找到解

### 对比测试
创建一个最简单的实例，测试基本性能：
```python
# 创建一个5任务5用户的简单实例
# 应该在<10ms内求解
```

如果连简单实例都慢，说明环境配置有问题。
